# These should only apply when <'ata>, <'eta> etc are full words, so I made them full-word rules
# Again I'm not sure if this is a good solution. It all depends on how we, that is you, are 
# going to resolve the matter of ordering the rules.

"ataToXta": lambda record: createMa(record, lambda tier: ("xta" if word=="qata" else word for word in tier)),
"etaToXta": lambda record: createMa(record, lambda tier: ("xta" if word=="qeta" else word for word in tier)),
"itaToXta": lambda record: createMa(record, lambda tier: ("xta" if word=="qita" else word for word in tier)),
"otaToXta": lambda record: createMa(record, lambda tier: ("xta" if word=="qota" else word for word in tier)),
"utaToXta": lambda record: createMa(record, lambda tier: ("xta" if word=="quta" else word for word in tier)),
"NgtaToXta": lambda record: createMa(record, lambda tier: ("xta" if word=="ngta" else word for word in tier)),